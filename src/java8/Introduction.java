package java8;

public class Introduction {



    // a pure example of function
    //function is return the same value when passed the same argument
      public String checkNum(int num) {
          return num%2 ==0 ? "even" : "odd";
      }

      // higher order function
}
